name: M&A Intelligence Auto-Update

on:
  schedule:
    # Каждые 2 часа для создания ощущения постоянной активности
    - cron: '0 */2 * * *'
  workflow_dispatch: # Ручной запуск для testing

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install requests matplotlib seaborn pandas
    
    - name: Generate M&A Intelligence & Breaking News
      run: |
        python generate_dashboard.py
        python generate_breaking_news.py
        
    - name: Commit and push M&A updates
      run: |
        git config --local user.email "intelligence@github.com"
        git config --local user.name "M&A Intelligence Bot"
        git add .
        if git diff --staged --quiet; then
          echo "No M&A activity detected"
        else
          TIMESTAMP=$(date +'%Y-%m-%d %H:%M UTC')
          git commit -m "� LIVE M&A Update: $TIMESTAMP

          ⚡ Breaking News Generated
          📊 Auto-generated market analysis
          🎯 Updated acquisition targets and valuations  
          💰 Fresh GitHub activity intelligence
          � Real-time priority alerts"
          git push
        fi
